name: "Quality check"

runs:
  using: "composite"
  steps:
    - uses: actions/checkout@v2
    - name: Setup node
      uses: actions/setup-node@v2
      with:
        node-version: ${{ matrix.node }}

    - name: Install
      run: yarn

    - name: Quality check
      run: yarn qualityCheck
